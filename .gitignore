# OS / editor cruft
.DS_Store
.obsidian/

# Reference rulebooks & large binaries (copyrighted source material — kept local only)
*.pdf
*.epub

# Scratch
*.tmp

# MkDocs build output (regenerated by CI on every push)
site/

# Compiled shareable editions are kept (override the *.pdf rule above)
!Shareable Editions/Player Primer/player-primer.pdf
